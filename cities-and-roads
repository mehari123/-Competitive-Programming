size = int(input())
num_roads = set()

for row in range(1,size + 1):
    
    row_ls = input().strip().split()
    
    for index,c in enumerate(row_ls):
       
        if c == "1":
            
            num_roads.add(index + 1)
            
print(len(num_roads))